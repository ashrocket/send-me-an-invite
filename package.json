{
  "name": "send-me-an-invite",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "bin": {
    "agentical-mcp": "./mcp/server.js"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "deploy": "npm run build && wrangler pages deploy dist",
    "mcp": "node mcp/server.js",
    "setup-oauth": "node scripts/setup-oauth.js"
  },
  "devDependencies": {
    "vite": "^6.2.0",
    "vitest": "^3.1.0",
    "wrangler": "^4.42.0"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "lottie-web": "^5.13.0"
  }
}
